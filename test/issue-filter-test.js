const { assert } = require('chai');
const { describe, it } = require('mocha');


describe('issue-filter', function() {
  describe('getRepo', function() {
    it('works', function() {
      assert.strictEqual(true, true, 'Hello world!');
    })
  });
});